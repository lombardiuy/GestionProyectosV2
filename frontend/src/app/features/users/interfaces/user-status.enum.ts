export enum UserStatus {
  Active = 'active', //Activo => con contraseña definida por usuario.
  Inactive = 'inactive', //Inactivo => Contraseña por defecto a cambiar en activación.
  Suspended = 'suspended' // Suspendido 
}
